<h2>Editing Book</h2>
<% if @book.errors.any? %>
 <div class="alert alert-danger">
   <h2><%= pluralize(@book.errors.count, "error") %>errors prohibited this book from being saved:</h2>
      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
<% end %>

<%= form_with model:@book, local:true do |f| %>
<p>Title</p>
<%= f.text_field :title %>
<p>Body</p>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<% end %>

<div class="link">
<%= link_to "Show", book_path(@book),class:"index" %><span>|</span>
<%= link_to "Back", books_path,class:"index" %>
</div>
